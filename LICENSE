
> Bu README, projeyi “ciddi araç” gibi gösterir. İnsanlar yıldız atarken önce README’ye bakar.

---

# 2) “Tek komut” çalışsın: tracelens.py’yi CLI yapalım
Şimdilik minimum ama etkili bir CLI:

**tracelens.py** içeriğini şu hale getir:

```python
import argparse
from pathlib import Path

def scan(path: str):
    p = Path(path)
    if not p.exists():
        print(f"[!] Path not found: {p}")
        return

    # Placeholder - next step: parse .evtx files
    print("[+] TraceLens is alive.")
    print(f"[+] Target: {p.resolve()}")
    print("[!] Next: EVTX parsing + detections")

def main():
    parser = argparse.ArgumentParser(prog="tracelens", description="Windows EVTX triage tool")
    sub = parser.add_subparsers(dest="cmd")

    s = sub.add_parser("scan", help="Scan a folder containing EVTX files")
    s.add_argument("path", help="Path to EVTX folder")

    args = parser.parse_args()
    if args.cmd == "scan":
        scan(args.path)
    else:
        parser.print_help()

if __name__ == "__main__":
    main()
